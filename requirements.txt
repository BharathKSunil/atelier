# Pipeline (models) — index / cluster / series / score
torch>=2.2
torchvision>=0.17
insightface>=0.7.3        # SCRFD detector + ArcFace embeddings (default)
onnxruntime>=1.17         # insightface runtime
huggingface_hub~=0.20     # fetch AdaFace IR-101 ckpt (RECOGNITION_MODEL="adaface")
hdbscan>=0.8.33
mediapipe>=0.10.9
numpy>=1.24,<2.0
pillow>=10.0
scikit-learn>=1.3
tqdm>=4.66

# Web (server)
flask>=3.0

# DINOv2 is pulled at runtime via torch.hub (facebookresearch/dinov2)
