# Pipeline (models) — needed for 01_index / 02 / 03
torch>=2.2
torchvision>=0.17
insightface>=0.7.3        # RetinaFace detector + ArcFace embeddings (default backend)
onnxruntime>=1.17         # insightface runtime
facenet-pytorch>=2.5.3    # legacy MTCNN fallback (FACE_BACKEND="mtcnn")
huggingface_hub~=0.20     # fetch AdaFace IR-101 ckpt (RECOGNITION_MODEL="adaface")
hdbscan>=0.8.33
mediapipe>=0.10.9
numpy>=1.24,<2.0
pillow>=10.0
scikit-learn>=1.3
tqdm>=4.66

# Web (04_server)
flask>=3.0

# DINOv2 is pulled at runtime via torch.hub (facebookresearch/dinov2)
