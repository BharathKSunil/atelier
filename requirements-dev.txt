# Light deps to run the pure-logic unit tests (no torch/mediapipe needed)
pytest>=7.4
numpy>=1.24,<2.0
pillow>=10.0
flask>=3.0          # tests/test_server.py exercises the Flask security guards
ruff>=0.6           # lint + format (make lint / make format)
build>=1.2          # wheel + sdist (make build)
pre-commit>=3.5     # git hooks (.pre-commit-config.yaml)
